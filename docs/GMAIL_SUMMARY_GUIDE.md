# 📧 Gmail 요약 기능 가이드

## 개요

Morning Briefing v3는 **안 읽은 메일을 자동으로 가져와서 AI로 요약**하는 기능을 제공합니다.

두 가지 요약 방식을 선택할 수 있습니다:

1. **5줄 간결 요약** (기본값) - 빠르게 훑어보기
2. **상세 요약** - 액션 아이템과 우선순위 포함

---

## 🚀 빠른 시작

### 1. Gmail API 권한 설정

프로젝트에서 Google OAuth2 토큰을 발급받을 때 Gmail API 권한이 자동으로 포함됩니다.

```bash
npm run get-token
```

필요한 권한:
- `https://www.googleapis.com/auth/gmail.readonly` (메일 읽기)
- `https://www.googleapis.com/auth/gmail.send` (브리핑 이메일 발송)

### 2. 환경변수 설정

`.env` 파일에 다음을 추가:

```env
# Gmail 요약 방식 선택 (선택사항)
GMAIL_BRIEF_SUMMARY=true   # 5줄 요약 (기본값)
# GMAIL_BRIEF_SUMMARY=false  # 상세 요약

# 미읽음 메일 최대 개수 (선택사항)
UNREAD_MAX=15  # 기본값: 15개
```

### 3. 테스트 실행

```bash
npm run test:email
```

이 명령어는:
- 안 읽은 메일을 가져옵니다
- 5줄 요약과 상세 요약을 **모두** 생성하여 비교할 수 있습니다
- 터미널에 결과를 출력합니다

---

## 📊 요약 방식 비교

### 5줄 간결 요약 (기본값)

**특징:**
- ✅ 가장 중요한 메일 5개만 선별
- ✅ 각 메일을 한 줄로 간결하게 요약
- ✅ 빠르게 훑어보기 좋음
- ✅ OpenAI 토큰 사용량 적음 (~500 tokens)
- ✅ 이메일이나 알림에 포함하기 적합

**예시:**

```
1. Amazon: 주문 배송 완료 - 오늘 오후 3시 도착 예정
2. GitHub: Pull Request 승인 요청 - morningbriefv3 저장소 리뷰 필요
3. LinkedIn: 새로운 연결 요청 - 김철수님이 연결을 요청했습니다
4. Notion: 팀 워크스페이스 초대 - 프로젝트 협업 공간 참여 필요
5. Google Calendar: 내일 회의 리마인더 - 10:00 AM 팀 미팅
```

### 상세 요약

**특징:**
- ✅ 각 메일의 액션 아이템 포함 (Reply/Review/Track/Ignore)
- ✅ 긴급/중요 메일 Top 3 강조
- ✅ 최대 10개 메일 상세 분석
- ⚠️ OpenAI 토큰 사용량 많음 (~2,000 tokens)
- ✅ 업무용으로 자세한 정보가 필요할 때 적합

**예시:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━
📧 미읽음 메일 요약 (총 12건)
━━━━━━━━━━━━━━━━━━━━━━━━━

1. From: Amazon
   Subject: 주문 배송 완료
   What it is: 주문하신 상품이 배송 완료되었습니다
   Action: Track - 배송 위치 확인 후 수령

2. From: GitHub
   Subject: Pull Request 승인 요청
   What it is: morningbriefv3 저장소에 새로운 PR이 생성됨
   Action: Review - 코드 리뷰 및 승인 필요 (우선순위 높음)

3. From: LinkedIn
   Subject: 새로운 연결 요청
   What it is: 김철수님이 LinkedIn에서 연결을 요청함
   Action: Ignore - 나중에 확인 가능

━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 긴급/중요 메일 Top 3
━━━━━━━━━━━━━━━━━━━━━━━━━

1. GitHub Pull Request - 코드 리뷰 필요 (오늘 중)
2. Google Calendar - 내일 중요 회의 (10:00 AM)
3. Slack - 팀 긴급 공지 (즉시 확인 필요)
```

---

## ⚙️ 설정 옵션

### 환경변수

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `GMAIL_BRIEF_SUMMARY` | `true` | `true`: 5줄 요약, `false`: 상세 요약 |
| `UNREAD_MAX` | `15` | 가져올 미읽음 메일 최대 개수 |

### Gmail 검색 쿼리

기본 검색 쿼리는 다음과 같습니다:

```javascript
const query = 'in:inbox is:unread newer_than:2d';
```

**의미:**
- `in:inbox`: 받은편지함에 있는 메일만
- `is:unread`: 읽지 않은 메일만
- `newer_than:2d`: 최근 2일 이내 메일만

**커스터마이징:**

`src/services/gmail.js` 파일에서 쿼리를 수정할 수 있습니다:

```javascript
// 예: 최근 1일 이내, 중요 메일만
const query = 'in:inbox is:unread is:important newer_than:1d';

// 예: 특정 발신자 제외
const query = 'in:inbox is:unread -from:noreply@example.com newer_than:2d';
```

---

## 🔒 개인정보 보호

Gmail 요약 기능은 **개인정보 보호**를 최우선으로 합니다.

### 자동 마스킹

메일 내용에서 다음 정보를 자동으로 마스킹합니다:

1. **이메일 주소**: `user@example.com` → `***@example.com`
2. **전화번호**: `010-1234-5678` → `010-****-****`
3. **계좌번호**: `1234567890123456` → `************`

### 내용 길이 제한

- 메일당 최대 1,500자까지만 읽어옵니다
- 5줄 요약 시 각 메일당 최대 500자만 OpenAI로 전송

### 권장사항

- 민감한 내용이 포함된 메일은 Gmail 필터로 제외하세요
- `.env` 파일은 절대 커밋하지 마세요 (`.gitignore`에 포함됨)
- GitHub Secrets를 사용하여 API 키를 안전하게 관리하세요

---

## 📝 API 사용량 및 비용

### OpenAI API 토큰 사용량

| 요약 방식 | 입력 토큰 | 출력 토큰 | 총 토큰 | 비용 (1일) |
|-----------|-----------|-----------|---------|------------|
| 5줄 요약 | ~300 | ~200 | ~500 | ~$0.0005 |
| 상세 요약 | ~1,500 | ~500 | ~2,000 | ~$0.002 |

**월 예상 비용:**
- 5줄 요약: ~$0.015/월
- 상세 요약: ~$0.06/월

*GPT-4o-mini 기준 ($0.15/1M input tokens, $0.60/1M output tokens)*

### Gmail API 할당량

- **읽기**: 1,000,000,000 quota units/day (무료)
- **하루 조회**: 15건 × 1일 = 15 API calls
- **사용률**: 0.000001% (매우 낮음)

---

## 🧪 테스트 및 디버깅

### 테스트 명령어

```bash
# Gmail 요약 기능만 테스트
npm run test:email

# 전체 브리핑 실행
npm start
```

### 로그 확인

```bash
# 로그 레벨 조정 (.env)
LOG_LEVEL=debug  # debug, info, warn, error
```

### 자주 발생하는 문제

#### 1. "미읽음 메일을 불러오지 못했습니다"

**원인:**
- Gmail API 권한이 없음
- OAuth2 토큰이 만료됨

**해결:**
```bash
npm run get-token  # 토큰 재발급
```

#### 2. "Gmail 요약 실패"

**원인:**
- OpenAI API 키가 없거나 잘못됨
- API 할당량 초과

**해결:**
```env
# .env 파일 확인
OPENAI_API_KEY=sk-...  # 올바른 키 입력
```

#### 3. 메일이 0건으로 표시됨

**원인:**
- 실제로 미읽음 메일이 없음
- Gmail 검색 쿼리가 너무 제한적

**해결:**
```javascript
// src/services/gmail.js에서 쿼리 수정
const query = 'in:inbox is:unread';  // 날짜 제한 제거
```

---

## 🎯 사용 시나리오

### 시나리오 1: 아침 출근길

**요구사항:**
- 빠르게 중요 메일만 확인
- 모바일에서 읽기 편한 형식

**설정:**
```env
GMAIL_BRIEF_SUMMARY=true  # 5줄 요약
UNREAD_MAX=5              # 최대 5개만
```

**결과:**
- 출근길 2분 안에 메일 확인 완료
- 중요한 메일만 빠르게 파악

### 시나리오 2: 업무 시작 전

**요구사항:**
- 각 메일에 대한 액션 아이템 필요
- 우선순위 파악

**설정:**
```env
GMAIL_BRIEF_SUMMARY=false  # 상세 요약
UNREAD_MAX=15               # 최대 15개
```

**결과:**
- 어떤 메일부터 처리할지 명확
- Reply/Review/Track/Ignore 액션 제시

### 시나리오 3: 주말 메일 정리

**요구사항:**
- 주말 동안 쌓인 메일 한 번에 확인
- 긴급 메일만 빠르게 필터링

**설정:**
```javascript
// src/services/gmail.js 수정
const query = 'in:inbox is:unread newer_than:3d';  // 3일로 확장
```

```env
GMAIL_BRIEF_SUMMARY=true
UNREAD_MAX=20
```

---

## 🔧 고급 커스터마이징

### 1. OpenAI 프롬프트 수정

더 나은 요약을 위해 프롬프트를 커스터마이징할 수 있습니다.

**파일:** `src/services/openai.js`

```javascript
// 5줄 요약 프롬프트 수정 예시
const prompt = `당신은 개인 비서입니다. 아래 미읽음 메일들을 분석하여 **정확히 5줄로** 요약해주세요.

${emailText}

**요구사항:**
- 업무 관련 메일을 최우선으로 선별
- 긴급도에 따라 정렬
- 이모지를 사용하여 카테고리 표시 (🔴 긴급, 🟡 중요, 🔵 일반)
...
`;
```

### 2. 메일 필터링

특정 조건의 메일만 포함하거나 제외:

```javascript
// src/services/gmail.js에서 파싱 후 필터링
const emails = emailResponses
  .map(response => parseEmail(response.data))
  .filter(email => {
    // 예: 프로모션 메일 제외
    return !email.subject.includes('프로모션') &&
           !email.from.includes('noreply');
  });
```

### 3. 다중 언어 지원

영어 메일을 한국어로 요약:

```javascript
const prompt = `...
주의사항:
- 모든 메일을 한국어로 요약
- 영어 제목은 한국어로 번역
...`;
```

---

## 📚 참고 자료

- [Gmail API 문서](https://developers.google.com/gmail/api)
- [OpenAI API 문서](https://platform.openai.com/docs/api-reference)
- [Google OAuth2 가이드](https://developers.google.com/identity/protocols/oauth2)

---

## 💡 팁과 트릭

### 팁 1: 중요 메일 우선 표시

Gmail 라벨을 활용하여 중요 메일만 가져오기:

```javascript
const query = 'in:inbox is:unread label:important newer_than:2d';
```

### 팁 2: 특정 시간대 메일만

```javascript
const query = 'in:inbox is:unread after:2026/02/12 before:2026/02/13';
```

### 팁 3: 토큰 사용량 모니터링

```javascript
// src/services/openai.js
logger.info('토큰 사용량', {
  inputTokens: completion.usage.prompt_tokens,
  outputTokens: completion.usage.completion_tokens,
  totalTokens: completion.usage.total_tokens,
  estimatedCost: (completion.usage.total_tokens / 1000000 * 0.15).toFixed(4)
});
```

---

## ❓ FAQ

### Q1: 메일을 읽음 처리하나요?

**A:** 아니요. 읽기 전용 권한(`gmail.readonly`)만 사용하므로 메일 상태를 변경하지 않습니다.

### Q2: 첨부파일도 요약하나요?

**A:** 아니요. 메일 본문만 요약합니다. 첨부파일은 다운로드하지 않습니다.

### Q3: 모든 메일함을 검색하나요?

**A:** 기본적으로 받은편지함(`in:inbox`)만 검색합니다. 쿼리를 수정하여 다른 메일함도 포함할 수 있습니다.

### Q4: HTML 메일도 요약하나요?

**A:** 네. HTML 태그를 제거한 후 텍스트만 추출하여 요약합니다.

### Q5: 비용이 얼마나 드나요?

**A:** 5줄 요약 기준 하루 $0.0005, 월 $0.015 정도입니다. (거의 무료 수준)

---

**Made with ❤️ by Morning Briefing v3**
